# LCFU 雷达配置工具 (GUI 版本)

一个用于 LCFU 雷达设备配置读写的图形界面工具。

## 项目结构

```
lcfu_gui/
├── lcfu_protocol.rkt      # 协议公共模块（字节序转换、CRC32等）
├── lcfu_write_lib.rkt     # 写协议核心库
├── lcfu_read_lib.rkt      # 读协议核心库
├── lcfu_gui_main.rkt      # GUI 主程序
└── README.md              # 本文件
```

## 模块说明

### 1. lcfu_protocol.rkt
公共协议模块，提供：
- Big Endian / Little Endian 字节序转换
- CRC32 校验计算
- 校验和计算
- 辅助函数（文件大小格式化等）

### 2. lcfu_write_lib.rkt
写协议库，实现：
- 6 步写入协议（握手、升级、擦除、传输、完成、校验）
- 带进度回调和日志回调
- 自动重试机制
- 异常处理

### 3. lcfu_read_lib.rkt
读协议库，实现：
- 分包读取协议
- 数据验证功能
- 带进度回调和日志回调
- 自动重试机制

### 4. lcfu_gui_main.rkt
GUI 主程序，提供：
- 连接设置界面
- 写入/读取双面板界面
- 实时日志显示
- 进度条显示
- 状态栏信息
- 文件浏览和保存

## 运行方法

```bash
# 运行 GUI 程序
racket lcfu_gui_main.rkt
```

## 功能特性

### 写入配置 (Write)
1. 选择配置文件（显示大小和 CRC32）
2. 配置块大小（默认 1024 bytes）
3. 点击 "开始写入配置" 执行以下步骤：
   - 握手验证
   - 升级请求
   - Flash 擦除
   - 数据传输（带进度显示）
   - 传输完成确认
   - 完整性校验

### 读取配置 (Read)
1. 选择验证文件（可选，用于验证读取数据）
2. 指定保存文件名
3. 配置块大小（默认 1400 bytes）
4. 点击 "开始读取配置" 执行：
   - 分包读取数据
   - 自动组装完整文件
   - 数据验证（如果提供验证文件）
   - 保存到本地文件

### 连接设置
- 目标 IP 地址（默认：192.168.1.10）
- 写端口（默认：8082）
- 读端口（默认：8083）
- 超时时间（5 秒）
- 最大重试次数（3 次）

### 日志功能
- 实时显示操作过程
- 带时间戳
- 带状态标识（✓ 成功、✗ 错误、⚠ 警告）
- 支持清空和保存到文件

## 协议说明

### 写协议（Big Endian, UDP 8082）
- 协议头：0xAAAA5555
- 6 步流程：握手 → 升级 → 擦除 → 数据 → 完成 → 校验
- 数据块传输带确认机制

### 读协议（Little Endian, UDP 8083）
- 请求标志：0xA5A5A5A5
- 响应标志：0x5A5A5A5A
- 分包读取，自动组装
- 支持数据验证

## 错误处理

程序实现了完善的错误处理：
- 超时异常（exn:lcfu:timeout）
- 协议异常（exn:lcfu:protocol）
- IO 异常（exn:lcfu:io）
- 验证异常（exn:lcfu:verify）

所有错误都会在日志中显示，并弹出提示框。

## 设计特点

1. **模块化设计**：拆分为协议、写库、读库、GUI 四个模块
2. **回调机制**：支持日志回调和进度回调
3. **线程安全**：GUI 操作在主线程，协议操作在工作线程
4. **用户友好**：图形界面、实时日志、进度显示
5. **错误提示**：友好的错误消息和状态提示

## 依赖

- Racket 7.0 或更高版本
- racket/gui 库（Racket 自带）

## 注意事项

1. 确保目标设备 IP 和端口配置正确
2. 写入操作会擦除设备 Flash，请谨慎操作
3. 读取验证功能可确保数据完整性
4. 操作过程中请勿关闭程序
5. 建议在操作前保存日志以便调试